<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.patilparagp.wfa"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="14"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:label="@string/app_name" android:icon="@drawable/netapp_wfa">

        <activity android:name="WorkflowListingActivity"
                  android:label="@string/app_name">
        </activity>

        <activity android:name="LoginAcivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <activity android:name="ExecuteActivity"
                  android:label="@string/app_name">
        </activity>
        <service android:name="RestApiCallingService">

        </service>

    </application>
</manifest>
